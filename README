Install it with:

$ go get github.com/ryanlbrown/hipster-serve

Then run hipster-serve in the directory you want to serve from:

$ hipster-serve

Configure your commands at localhost:8080/

Access the files at localhost:8080/path/relative/to/serve/directory

TODO: write a real README
